services:
  - type: worker
    name: finreach-worker
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python data_generation.py && python rfm_ab_testing.py && python ai_action_engine.py"
    # Note: Configure GEMINI_API_KEY in the Render dashboard
